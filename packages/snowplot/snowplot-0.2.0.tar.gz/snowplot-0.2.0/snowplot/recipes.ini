[lyte_probe_recipe]
lyte_probe_trigger:
  has_section = lyte_probe
lyte_probe:
  apply_defaults = True
output:
  apply_defaults = True

[snow_micropen_recipe]
snow_micropen_trigger:
  has_section = snow_micropen
snow_micropen:
  apply_defaults = True
output:
  apply_defaults = True

[hand_hardness_recipe]
snow_micropen_trigger:
  has_section = hand_hardness
hand_hardness:
  apply_defaults = True
output:
  apply_defaults = True

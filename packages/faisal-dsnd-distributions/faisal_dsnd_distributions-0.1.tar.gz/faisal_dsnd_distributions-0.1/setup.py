from setuptools import setup

setup(name='faisal_dsnd_distributions',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['faisal_dsnd_distributions'],
      zip_safe=False)
